[package]
edition = "2018"
license = "MIT"
name = "node_api"
publish = true
version = "0.0.0"

[lib]
path = "lib.rs"

[features]
default = []
serde_1 = ["serde"]

[dependencies]
node_api_macro = { path = "macro" }
node_api_sys = { path = "sys" }
num = "0.4"
serde = { version = "1", optional = true }
tangram_error = { path = "../error" }
